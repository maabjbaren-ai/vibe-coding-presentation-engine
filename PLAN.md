# PLAN.md - תוכנית עבודה אטומית
# שם הקבוצה: mm-biu26

## שלב 1: אפיון ותשתיות ניהול (מבוצע)
* יצירת קובצי PRD.md ו-PLAN.md בשורש הפרויקט כדי להגדיר חוקי RTL ובידי.
* הגדרת קובץ TODO.md למעקב משימות שוטף.

## שלב 2: כתיבת מפרט התוכן (JSON Specification) (מבוצע)
* יצירת קובץ `slides_content.json` המכיל בדיוק 20 שקופיות המכסות את חומר הלימוד.
* וידוא שחצי מהשקופיות (10 שקופיות) הן בעברית מלאה ותקנית.

## שלב 3: הפקת הפסקול (Suno V5)
* כתיבת ליריקה מבוססת חרוז נטוי (Slant Rhymes) על עולם הניהול וה-AI.
* הפקת הרצועה המוזיקלית ב-Suno ושילוב קובץ ה-MP3 במערכת.

## שלב 4: פיתוח תבנית Reveal.js וארכיטקטורת RTL CSS
* הקמת שלד HTML עם ספריות Reveal.js.
* כתיבת חוקי CSS תשתיתיים לעברית: הגדרת `dir="rtl"`, מנגנון `unicode-bidi: embed`, וביטול רשימות שבורות לטובת מבני `display: table`.

## שלב 5: מנוע הזרקת התוכן (Python Script) והרצה
* כתיבת סקריפט Python הקורא את ה-JSON ומזריק אותו אוטומטית לתוך תבנית ה-HTML.
* הרצה, תיקונים, תיעוד ב-README.md עם צילומי מסך והערכת עלות טוקנים.
